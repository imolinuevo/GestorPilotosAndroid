<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="miw.dasm.inigo.gestorpilotos" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_miw"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ActividadPrincipal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetallePilotoActivity"
            android:label="@string/title_activity_detalle_piloto"
            android:parentActivityName=".ActividadPrincipal"
            android:theme="@style/AppTheme.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="miw.dasm.inigo.gestorpilotos.ActividadPrincipal" />
        </activity>
    </application>

</manifest>
